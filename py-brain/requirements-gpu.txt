# GPU/RAPIDS extras — install ONLY on a machine with an NVIDIA GPU + matching
# CUDA toolkit. Layered on top of the base requirements:
#   pip install -r requirements.txt -r requirements-gpu.txt
#
# pip syntax note: a per-index option (--index-url / --extra-index-url) must
# be on its own line; it cannot be appended after a requirement on the same
# line (the old `torch>=2.1.0 --index-url ...` form is invalid and breaks
# `pip install -r requirements.txt`). See:
# https://pip.pypa.io/en/stable/reference/requirements-file-format/
--extra-index-url https://download.pytorch.org/whl/cu118
--extra-index-url https://pypi.nvidia.com

torch==2.1.0+cu118
cudf-cu11==23.10.*
dask-cudf-cu11==23.10.*
